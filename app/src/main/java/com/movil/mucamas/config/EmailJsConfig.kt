package com.movil.mucamas.config

object EmailJsConfig {

    // IMPORTANTE: Reemplaza estos valores con tus credenciales reales.
    // Para mayor seguridad, considera cargarlos desde un archivo local.properties
    // que no se incluya en el control de versiones.
    const val SERVICE_ID = "YOUR_SERVICE_ID"
    const val TEMPLATE_ID = "YOUR_TEMPLATE_ID"
    const val PUBLIC_KEY = "YOUR_PUBLIC_KEY"
}
